[Unit]
# Clear the Red Hat dracut condition since Arch mkinitcpio doesn't create it
ConditionPathExists=

[Service]
# Clear the default ExecStart that has --apply (forces reboot)
ExecStart=
# Stage updates in background WITHOUT rebooting. Applied on next natural reboot.
ExecStart=/usr/bin/bootc upgrade --quiet
